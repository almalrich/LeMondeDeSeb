{% extends 'base.html.twig' %}

{% block title %}Vin{% endblock %}

{% block body %}
    <h1>Vin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vin.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vin.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ vin.color }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ vin.description }}</td>
            </tr>
            <tr>
                <th>MetId</th>
                <td>{{ vin.metId }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ vin.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vin_index') }}">back to list</a>

    <a href="{{ path('vin_edit', {'id': vin.id}) }}">edit</a>

    {{ include('vin/_delete_form.html.twig') }}
{% endblock %}
