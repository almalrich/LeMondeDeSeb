<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        {% block stylesheets %}<link rel="stylesheet" href="{{ asset("MaCave.css") }}">{% endblock %}
        <meta content="width=devise-width, initial-scale=1" name="viewport"/>
        <title>{% block title %}mondeDeSeb{% endblock %}</title>
        <h1>Le Monde De Seb</h1>
    <div class="user-info">

        <span>....</span>

        <a class="delog" href="{{ logout_path('main') }}">deconnection</a>

        <a class="btn-retour" href {{ path ('homepage') }}">acceuil</a>

        <a class="btn-user" href="{{ path('baseUser') }}">VinUser</a>

    </div>
    <header>{% block header %}



        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <div class="strat">
                <p>
                    <strong id="bienvenu">

                        Bienvenue, <span>{{ app.user.username }}</span>

                    </strong>
                </p>

            </div>

        {% endif %}

        <main class="container">

            <nav class="barre">

                <div class="menu">



                    <a id="rouge" href="{{ path('vin_rouge') }}">Rouge</a>

                    <a id="rose" href="{{ path('vin_rose') }}">Rosé</a>

                    <a id="blanc" href="{{ path('vin_blanc') }}">Blanc</a>

                    <a id="pet" href="{{ path('vin_petillant') }}">Petillant</a>



                </div>

            </nav>

        </main>


    </header>{% endblock %}


    {% if is_granted('ROLE_ADMIN') %}

    <a class="vin-index" href="{{ path ('vin_index') }}">Vins</a>

    <a class="mets-index" href="{{ path ('mets_index') }}">mets</a>

    {% endif %}
    <body>
    {% block body %}{% endblock %}
    {% block top_scripts %}
    {% endblock %}

    {% block javascripts %}{% endblock %}
    </body>
</html>
