{% extends 'base.html.twig' %}

{% block title %}Edit Mets{% endblock %}

{% block body %}

    <h1>Edit Mets</h1>

    {{ include('mets/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn-retour" href="{{ path('mets_index') }}">back to list</a>

    {{ include('mets/_delete_form.html.twig') }}
{% endblock %}
